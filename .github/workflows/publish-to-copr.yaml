name: Publish to Copr

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'New version. Leave blank to auto-bump the patch.'
        required: false
        type: string

permissions:
  contents: write

env:
  TOOL: atlas-rt

jobs:
  tito-tag:
    runs-on: ubuntu-latest
    container:
      image: fedora:latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          fetch-tags: true
          persist-credentials: true

      - name: Install Dependencies
        run: |
          dnf install -y rpm rpm-build make git python3-pip
          dnf install -y tito copr-cli
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Tito check
        run: |
          tito --version

      - name: Test Build (tito --test)
        run: |
          OUTDIR="${RUNNER_TEMP}/tito-test"
          mkdir -p "$OUTDIR"
          tito build --test --rpm --output="$OUTDIR"
          ls -la "$OUTDIR"

      - name: Resolve Version
        id: ver
        run: |
          raw='${{ inputs.version }}'
          raw="${raw#v}"
          raw="${raw#atlas-rt-}"
          # Drop a trailing -N release suffix if someone passed a full NVR.
          if [[ "$raw" =~ ^([0-9]+\.[0-9]+\.[0-9]+) ]]; then
            echo "version=${BASH_REMATCH[1]}" >> "$GITHUB_OUTPUT"
          else
            echo "version=" >> "$GITHUB_OUTPUT"
          fi
          echo "input raw='${{ inputs.version }}' resolved='$(cat "$GITHUB_OUTPUT" | sed -n 's/^version=//p')'"

      - name: Tito Tag
        run: |
          if [ -n "${{ steps.ver.outputs.version }}" ]; then
            tito tag --use-version="${{ steps.ver.outputs.version }}" --accept-auto-changelog
          else
            tito tag --accept-auto-changelog
          fi

      - name: Show new Tag
        run: |
          echo "tito packages:"
          cat ".tito/packages/${TOOL}"
          echo "new tag: $(git describe --tags --abbrev=0)"
          git log -1 --oneline

      - name: Push commit and tag
        run: git push --follow-tags origin
